<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.ipp.estgf.quizdroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".niveis"/>
        <activity android:name=".Resultados_Activity"/>
        <activity android:name=".ranking"/>
        <activity android:name=".administracao"/>
        <activity android:name=".Pergunta"/>
        <activity android:name=".PerguntaActivity"/>


    </application>

</manifest>